name: tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Miniconda
        uses: conda-incubator/setup-miniconda@v3
        with:
          activate-environment: qa-eval
          environment-file: environment.yml
          auto-activate-base: false

      - name: Run simple tests
        shell: bash -l {0}
        run: |
          conda activate qa-eval
          poetry install --no-interaction --with=test
          poetry run pytest --cov=qa_eval --cov-report=term-missing tests/

      - name: Run tests dependent on OpenAI
        shell: bash -l {0}
        run: |
          conda activate qa-eval
          poetry install --no-interaction --with=test --with=openai
          poetry run pytest --cov=qa_eval --cov-report=term-missing tests-with-openai
